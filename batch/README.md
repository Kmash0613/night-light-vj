# バッチ前処理ツール（Python）

Phase 3 で実装するコンポーネント。要件定義書 §4, §9 参照。

現時点ではディレクトリ構成のみを用意している（未実装）。

```
batch/
  input/            # 夜景写真の投入先
  scenes/           # 前処理済み出力（color.jpg / depth.png / mask.png / meta.json）
  src/              # パイプライン実装（正規化・深度推定・光点抽出・クラスタリング等）
  requirements.txt
```

## 実装順序（要件定義書 §9 と対応）

1. Phase 1〜2 で手作業確認した内容をもとに、まず「正規化 → 光点抽出」の最小パイプラインを実装
2. Depth Anything V2 を組み込み、深度推定・後処理を追加
3. クラスタリング・カメラパステンプレート選択・品質フラグ・コンタクトシート出力を追加

## 未決事項（着手前に確認）

- Depth Anything V2 のモデルサイズとライセンス
- CPU実行時の深度推定所要時間
